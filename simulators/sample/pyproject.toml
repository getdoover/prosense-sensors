[project]
name = "prosense-sensors-sim"
version = "0.1.0"
description = "Simulator that emits ProSense-shaped raw register values via tags."
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.0.0a8",
]

[project.scripts]
doover-app-run = "main:main"
